# Contributing to `defcli`

## Tests

```
$ git clone https://github.com/bfontaine/defcli.git
$ cd defcli
$ bundle install
$ bundle exec rake test
```

It’ll generate a `coverage/index.html`, which you can open in a Web browser.
